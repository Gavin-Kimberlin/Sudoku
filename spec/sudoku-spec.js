describe('array', function() {

  it('should only have numbers 1-9', function() {
    var column1 = new Column(1);
     column.forEach(function (array) {
       return (new Set(array)).size !== array.length;
    });
  });
});
