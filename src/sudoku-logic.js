export function checkRow (row) {
  row.forEach(function (array) {
    return (new Set(array)).size !== array.length
  })
};

export function checkColumn (column) {
  row.forEach(function (array) {
    return (new Set(array)).size !== array.length
  })
};

export function checkBox (box) {
  row.forEach(function (array) {
    return (new Set(array)).size !== array.length
  })
};
